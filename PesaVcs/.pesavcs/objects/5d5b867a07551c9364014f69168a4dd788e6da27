namespace PesaVcs.Core.Interfaces
{
    public interface IRemoteRepositoryService
    {
        void Push(string remoteUrl, string branch);
        void Pull(string remoteUrl, string branch);
        void Fetch(string remoteUrl);
        void SetRemoteUrl(string remoteName, string remoteUrl);
    }
}
